TODO:

see if vm's ip can be added to dns (keep dynamic ips but have a static fqdn)
add fqdns to ansible for inventory
set up nfs on nfs server
see how to use nfs server as storage class
set up registry
